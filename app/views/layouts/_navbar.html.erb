<header class="mb-4">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <a class="navbar-brand" href="/">ken's blog</a>
    <!-- 横幅が狭い時に出るハンバーガーボタン -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- メニュー項目 -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <% if current_user.try(:admin?) %>
          <li class="nav-item"><%= link_to  '新規投稿', new_article_path, class: 'nav-link' %></li>
        <% end %>
          <li class="nav-item"><%= link_to  'ユーザー設定', edit_user_registration_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to  'logout', logout_path , class: 'nav-link' %></li>
      </ul>
    </div>
  </nav>
</header>